# maybe borrow the black table cloth
# s-shaped!
columns = 23
rows = 12
# night sky
# 0-22 45-23 46-68 ...
# 
# pitch: speed to reach the brightest state
# the higher the faster
# max brightness -- volume   & 2 to 3 layers

# which coordinate is random
# background color dark blue

# layer 1: index    & add a little bit of orange/green/pink on the indexes (random color)                             effect(index)
# layer 2 ( if within bounds): index+1, index-1, index + 23, index -23            effect(index)
# layer 3: for every index from layer 2, do the same thing              effect(index)

def effect(index):
    brightness += 10
    yellow += 10
def fade():
    #fade to dark blue slowly